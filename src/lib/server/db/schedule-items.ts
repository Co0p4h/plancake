import * as table from '$lib/server/db/schema';
import { db } from "./index";
import { eq } from 'drizzle-orm';
import { generateId } from './utils';

// create schedule item 


export async function createScheduleItem(user, item) {
}